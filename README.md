# Placeform

The working architectural studio is in [`studio`](studio/README.md).

```sh
cd studio
npm ci
npm run dev -- --host 0.0.0.0 --port 3001
```

The prepared Portland demonstration works without API keys. See the studio README for optional OpenAI voice and AIand video connections, exports, research handoffs, and validation.
